# ScopePerspective swaps mc.framebufferMc to the scope FBO during scope rendering
# so that all code paths (including Angelica/Iris pipeline compositing) that reference
# "the main framebuffer" transparently target the scope FBO.
public net.minecraft.client.Minecraft field_147124_at # framebufferMc
