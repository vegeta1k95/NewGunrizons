# WeaponWorldRenderer overrides renderHand as a no-op so the scope framebuffer
# doesn't contain the player's hand/weapon model. The method is private in vanilla,
# so we need an AT to make it overridable.
public net.minecraft.client.renderer.EntityRenderer func_78476_b(FI)V # renderHand
